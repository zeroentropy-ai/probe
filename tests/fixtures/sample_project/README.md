# Sample Project

A sample project for testing probe indexing and search.

## Getting Started

1. Install dependencies: `pip install -r requirements.txt`
2. Run the server: `python -m server`
3. Visit http://localhost:8000
