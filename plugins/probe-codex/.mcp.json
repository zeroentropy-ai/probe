{
  "mcpServers": {
    "probe": {
      "command": "uvx",
      "args": [
        "--from",
        "probe-search==0.4.7",
        "probe",
        "mcp"
      ],
      "env": {
        "ZEROENTROPY_API_KEY": "${ZEROENTROPY_API_KEY}"
      }
    }
  }
}
