{
  "mcpServers": {
    "probe": {
      "command": "uvx",
      "args": [
        "--from",
        "probe-search==0.4.0",
        "probe",
        "mcp"
      ],
      "env": {
        "ZEROENTROPY_API_KEY": "${user_config.zeroentropy_api_key}"
      }
    }
  }
}
